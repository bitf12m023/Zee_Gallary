@{
    ViewBag.Title = "DeleteItem";
}

@model project.Models.NewItem

<!DOCTYPE html>


<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body style="background-image:url('../Content/chocolates/back.jpg')">
    <br /><br /><br /><br /><br />
    <div style="font-weight:bold;margin-left:10%">
        @using (Html.BeginForm("Delete", "Admin", FormMethod.Post))
        {
            @Html.HiddenFor(m => m.Id)
            <br /><br />
           
            @Html.LabelFor(m => m.name)
            @Html.EditorFor(m => m.name)
             <br /><br />


            @Html.LabelFor(m => m.desc)
            @Html.EditorFor(m => m.desc)
            @Html.LabelFor(m => m.path)
            @Html.EditorFor(m => m.path)

            <input type="submit" value="Delete Food" />
        }
    </div>
    
</body>
